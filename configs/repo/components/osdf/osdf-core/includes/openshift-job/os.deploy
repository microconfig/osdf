#include openshift-urls
#include osdf-version-labels
#include osdf-pod-deployment
#include osdf-component-settings

job:
  retries: 1
  duration: 40

#var microconfig.template.job.fromFile: ${openshift-job@configDir}/templates/job.yaml
#var microconfig.template.job.toFile: openshift/job.yaml
