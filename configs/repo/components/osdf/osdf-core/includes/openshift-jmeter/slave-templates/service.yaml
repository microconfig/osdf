kind: Service
apiVersion: v1
metadata:
  name: <CONFIG_NAME>-service
  labels:
    application: <CONFIG_NAME>
    configHash: <CONFIG_HASH>
spec:
  selector:
    application: <CONFIG_NAME>
  ports:
    - port: ${this@slave.port}
      protocol: TCP
      targetPort: ${this@slave.port}
      name: http-${this@slave.port}